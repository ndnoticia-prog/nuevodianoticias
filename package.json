{
  "name": "nd-platform",
  "version": "0.1.0-alpha.1",
  "private": true,
  "description": "ND Platform monorepo — asset build workspace for ND Theme and ND Builder.",
  "license": "GPL-2.0-or-later",
  "workspaces": [
    "packages/nd-theme",
    "packages/nd-builder"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx --fix",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "sass": "^1.81.0",
    "vite": "^5.4.11"
  }
}
